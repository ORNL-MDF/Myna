# Example workspaces

This directory contains example workspace files reused across multiple workflow
cases. These files provide shared settings such as application executables and other
common workflow configuration.
